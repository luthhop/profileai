{
  "name": "profileai",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "instalar": "npm install --prefix frontend && npm install --prefix backend"
  }
}
